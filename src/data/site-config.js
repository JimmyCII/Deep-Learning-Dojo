/**
 * Per-site configuration. Edit these before pushing to a public repo.
 * All values here are safe to expose to the browser (no secrets).
 */

export const SITE = {
  appName:    "Data Ninja Deep Learning Dojo",
  version:    "1.0.0",

  // Email address that the "Email Jim" feedback button targets.
  feedbackEmail: "jimcockerham@cox.net",

  // GitHub repo URL — used by the "Open a GitHub issue" feedback option.
  // Leave as empty string until the repo exists; the button will hide itself.
  repoUrl:    "",

  // Whether to show the feedback button in the topbar. Default: true.
  showFeedbackButton: true
};
